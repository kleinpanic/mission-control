# Mission Control Environment Configuration
# Copy to .env.local and fill in your values:
#   cp .env.example .env.local

# Server-side gateway URL (used by API routes)
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# Gateway authentication token (get from ~/.openclaw/openclaw.json)
OPENCLAW_GATEWAY_TOKEN=your-token-here
NEXT_PUBLIC_OPENCLAW_GATEWAY_TOKEN=your-token-here

# Server port (default: 3333)
MISSION_CONTROL_PORT=3333

# Client-side gateway URL override (OPTIONAL)
# Leave unset to auto-detect from browser hostname (recommended).
# The app will use ws://<your-hostname>:18789 automatically.
# Only set this if the gateway is on a different host/port than expected.
# NEXT_PUBLIC_OPENCLAW_GATEWAY_URL=ws://your-gateway-host:18789
